# API 对接进度审计报告

> 日期：2026-05-29 | 对应后端 openapi.json

---

## 已正确对接（可直接工作）

| 模块 | 状态 |
|:---|:---:|
| api/client.ts 响应拦截器 | ✅ |
| api/auth.ts (login/register/me) | ✅ |
| api/shops.ts (列表/详情/评分/菜单/收藏) | ✅ |
| api/dictionary.ts (dict/types, dict/data) | ✅ |
| api/comments.ts (评论CRUD + 回复) | ✅ |
| api/questions.ts (通过comments.ts导出) | ✅ |
| api/favorites.ts (toggle + 列表) | ✅ |
| api/notifications.ts (消息CRUD) | ✅ |
| api/activities.ts (动态) | ✅ |
| api/history.ts (浏览历史) | ✅ |
| types/shop.ts shop/comment/question | ✅ |
| HomePage.tsx | ✅ |
| ShopDetailPage.tsx | ✅ |

## 待修复的问题

### 🔴 第一批（必崩，优先修）

1. **ProfilePage.tsx** — `activity.shopId`→`shop_id`，`createdAt`→`created_at`，无`extra.score`
2. **NotificationsPage.tsx** — 字段映射：`isRead→is_read`, `createdAt→created_at`, `description→content`, `shopId→related_entity_id`
3. **HistoryPage.tsx + types/history.ts** — 后端返回 `ViewHistoryItem{shop_id,shop_name,shop_cover,region,viewed_at}`，不是 `ShopCardData`
4. **types/favorite.ts** — 后端返回 `FavoriteResponse{id,user_id,shop_id,shop_name,created_at}`，不是 `ShopCardData`
5. **QASection.tsx + CommentSection.tsx** — `.answers` / `.replies` 不存在于类型上

### 🟡 第二批（界面正常但数据错）

6. types/activity.ts — camelCase 字段改 snake_case
7. types/notification.ts — camelCase 字段改 snake_case
8. ShopDetailPage.tsx — 去除 `shop.description`

### 🟢 第三批（新功能对接）

9. UploadShopPage — 对接真实接口
10. admin/* 后台 — Phase 7 启动
